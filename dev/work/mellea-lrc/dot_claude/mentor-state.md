# Mentor state — mellea-lrc
Last updated: 2026-07-09

## Support levels
| Domain | Level | Last change (reason) |
|---|---|---|
| design | 3 | 2026-07-09: designed kind-discriminator union serialization unprompted from seed question; sound decision record |
| implementation | 3 | 2026-07-09: fixed all accepted review findings correctly, incl. better-than-suggested citation_id-keyed mapping |
| process | 2 | held: changes twice sat unrun until verification gate prompted |
| english | 2 | held: spelling gate activated (3 recurrences) |

## Open questions
<!-- id | question | answered / parked (unpark trigger) / dropped (reason) -->
Q1 | Run save_citations, predict + paste output | answered (ran full round-trip; hit equality bug)
Q2 | Where does citation type come from so load rebuilds right class? | answered (injects `kind` on save L45; `mapping` on load L51-72 — correct)
Q3 | List ALL fields ExtractedDocument ctor accepts incl inherited; which ride through **data unrebuilt & are dataclasses? | answered (found source_metadata from DocumentBase, fixed)
Q4 | print type() of that field on both objects, predict first, paste | answered (ran full main(); nothing printed → citation == citations; round-trip works)
Q5 | Would eq still pass if format left as str not SourceFormat(...)? | answered — conclusion right (yes) but reasoning wrong: claimed "Python doesn't compare class type." Corrected: dataclass __eq__ DOES check class (that was the bug); passes only b/c same-class-both-sides + str-Enum equality.
Q6 (2026-07-09) | Move save/load into library next to types | parked (unpark: after benchmark data flow milestone; user deprioritized deliberately)
Q7 (2026-07-09) | Per lifecycle (inputs / manual annotations / model outputs): in git? under data/? | answered — proposed data/citations/{manual,eyecite,mellea}, all in git ("need on all machines"); mentor pushed back on versioning regenerable model outputs (Q10)
Q8 (2026-07-09) | What's in the uncommitted pyproject.toml + uv.lock change; own commit? | answered — re-included benchmark/ in ruff config; BUT commit bundled ~1300-line uv.lock resolution change under unrelated message (process lesson: read the stat line)
Q9 (2026-07-09) | Run ruff over benchmarks/; predict then paste | answered — prediction (T201) confirmed BUT run surfaced 27 errors incl. unpredicted S501 verify=False + S113 no-timeout in test-spin.py, ERA001 in library src. Lesson: run > prediction.
Q10 (2026-07-09) | Staleness of committed model outputs? | answered by design change — ground truth in git, model outputs in .cache. Staleness dissolved.
Q11 (2026-07-09) | Decision record for benchmark data layout | answered — accepted & logged (see DECISIONS); missing "what was rejected" sentence, user to append. "everytime" survived confirmed reread → suggested tool-based spellcheck.
Q12 (2026-07-09, minor) | ground-truth naming | answered — ground_truth underscore, matches repo convention
Q13 (2026-07-09) | Triage the 27 ruff findings | partial — test-spin.py: won't merge (enforcement unresolved → Q16); mellea.py: restore parked (unpark: BEFORE MERGE); recall_bench.py findings unruled → Q15
Q14 (2026-07-09) | .cache path authoritative? | answered — verified benchmarks/Extraction/.cache/, ignore covers it, record path was a typo
Q15 (2026-07-09) | Dead diagnostic scaffolding in main() | answered 2026-07-13 — user gutted it, removed unused var, will rename recall_bench.py → generate_test_cases.py (honest naming; use git mv) + new run_bench.py for scoring. Ruff re-run owed (Q17).
Q16 (2026-07-09) | test-spin.py enforcement | answered — gitignored (verified), plotting/ too; .gitignore commit was pending 2026-07-13
Q17 (2026-07-13) | Re-run ruff; predict test-spin.py absence | answered — "All checks passed!" repo-wide + format clean. PROCESS NOTE: prediction step skipped 2nd time.
Q18 (2026-07-13) | Span alignment proof | half-answered — "using .cache text only" = provenance, not proof (browser paste / LS storage / export can all mutate text; LS offsets computed against LS's copy). Follow-up Q19.
Q19 (2026-07-13) | Per-citation span-alignment assertion | CLOSED — user stated assert text[span.start:span.end] == matched_text, in the converter. Pairs with doc-level hash. Teaching arc complete (hash→per-span).
Q20 (2026-07-13) | git status/log confirmation | answered — surfaced 3 findings: (a) rename R076 clean, (b) mellea.py commented-code restore DONE via 73685e8 (unparked early), (c) INCIDENT test-spin.py committed by 64e9dc3 despite ignore & pushed — remote personal/feat/extraction still contains it
Q21 (2026-07-13) | test-spin.py history ruling | evidence complete — mentor verified committed version: NO secrets (env-based keys; leaks only internal hostname adrian-ubuntu + verify=False pattern). User untracked via f3df1e0 (unpushed). One-line accept-vs-rewrite ruling still owed.
Q22 (2026-07-13) | Fix filename typo run_bechmarks.py → run_benchmarks.py | open — WORSENED: typo committed in f3df1e0; git mv + commit before anything imports it
Q23 (2026-07-13) | Label Studio "not saving" | DIAGNOSED (pending user confirmation) — contract mismatch: label_studio.py _field_results emits a textarea result per asdict(citation) field, but Woody's label_studio_config.xml only defines TextAreas {plaintiff,defendant,volume,reporter,page,pin_cite,extra,year,court,notes}. Missing at least: parenthetical (FullCase/FullLaw/FullJournal), publisher (FullLaw). LS rejects annotations w/ unknown from_name at save → Update silently fails. User to: (a) derive the diff themselves (Q25), (b) confirm via devtools Network tab 4xx on Update (Q26). Fix = design choice with Woody: add TextAreas vs stop emitting fields (changes GT richness vs UI).
Q24 (2026-07-13) | Labeling requirement 2-liner | dropped — LS recovered, ground truth produced; yardstick no longer needed
Q25 (2026-07-13) | What fixed LS? one line for the log (was it the schema mismatch? was Network-tab evidence captured?) | open
Q26 (2026-07-13) | GROUND TRUTH EXISTS: data/ground-truth/Korematsu_v_US.json — but (a) UNTRACKED (irreplaceable, commit NOW), (b) folder hyphen contradicts own decision record (ground_truth underscore) — rename | open, directives issued
Q27 (2026-07-13) | annotations vs predictions key | open — answer demanded before key is typed; SPRINT context
Q28 (2026-07-13) | Where does converter live? | deferred by scope guard — landed in benchmarks/Extraction/load_ground_truth.py for the sprint; proper home decided with Q6 later
Q29 (2026-07-13) | ExtractionBackend has only EYECITE — ground truth needs a value: extend enum w/ MANUAL="manual" vs placeholder+flag; user decides aloud | open — SPRINT
SPRINT (30 min, 2026-07-13): load_ground_truth.py: LS JSON → ExtractedDocument — ABORTED by user 2026-07-14 (converter "too many nuances", progress report deadline). PIVOT: recall-only GT-lite.
Q30 (2026-07-14) | GT-lite pivot | mentor vetoed hand-transcription (duplicates LS labor + typo hazard corrupts exact-match recall) and span-less ExtractedDocument (fabricated Spans betray type's promises; __post_init__ exists to prevent this). Redirect: filter LS JSON result entries type=="labels" → (value.labels[0], value.text) — minutes, typo-free, reuses labeling. GT-lite = plain list[(kind, text)], NOT ExtractedDocument.
Q31 CLOSED 2026-07-14 | Match predicate: NORMALIZE WHITESPACE ONLY (collapse runs, keep case/punctuation) — chosen via picker; implement as a named function in run_bench so the choice stays visible/changeable
Q21 CLOSED 2026-07-14 | test-spin.py history: ACCEPT, dies on squash-merge (no secrets verified; zero effort) — chosen via picker
Q27 CLOSED 2026-07-14 (the hard way) — user's stated path "annotations > prediction > result" WAS the trap: annotations[0]["prediction"] is a frozen eyecite pre-annotation copy. Mentor verified in LB_Korematsu_v_US.json: human result = 29 labels regions, prediction copy = 42; delta 33 spans (user rejected 23, added 10 = the labeling value). Correct path: annotations[0]["result"]. Evidence shown as table; lesson = provenance keys mimic data keys.
PIVOT 2 (2026-07-14): plot.py before real numbers. Two-track granted: A = plot.py on placeholders, MOCK label mandatory in title; B = GT-lite filter (correct key) + predicate + compare loop = real recall (~30 min). GT file renamed LB_Korematsu_v_US.json.
PLOT.PY REVIEW (2026-07-15): ran (recall_1.svg evidence). Q22 rename done, uncommitted. FINDINGS: (1) MOCK label missing, 4 bars from 1 self-compared number (=100% by construction) — directive issued; (2) Q32: tuple swap — get_recall returns (correct,total), main unpacks (total,correct); masked because self-comparison makes correct==total; (3) Q33: zip pairwise comparison breaks on first miss (positional shift) — recall = set-membership per GT citation; Q31 predicate (ws-normalize named fn) NOT implemented, raw == used; (4) spelling gate breach: "benchmakr","sytles","doesn't is incorrect" — recurrence #5, spellcheck before commit. META-LESSON taught: symmetric placeholder input structurally cannot catch swap/pairing bugs.
Q32 CLOSED 2026-07-15 | tuple swap trace: user answered 116% correctly via picker — understood inverted ratio + why self-comparison masked it
Q33 CLOSED 2026-07-15 | zip trace: user answered 0/3-should-be-2/3 correctly via picker — understood positional vs membership
FIX LIST issued for plot.py: unpack order → membership recall w/ Q31 normalize() named fn → MOCK title → spellcheck. Then GT-lite deserializer = first honest recall number.
NOTE: user's client had rendering issues 2x (prose not displayed; picker works) — suggested /doctor; prefer picker for key questions until resolved. RECURRENCE #3 (2026-07-16): user complained "output quite small / not much help" right after the LONGEST message of the session (full benchmark plan) — plan text almost certainly not rendered (it preceded tool calls). Mitigation adopted: put full content in FINAL message after all tool calls. /doctor re-suggested.
PR MERGED (2026-07-20) — MILESTONE: first substantial merge to main (library+LS tooling+tests+GT). Idea sweep done: three-backends = nearly ripe (gate: first smoke run); hero chart = still parked (needs real numbers); chezmoi = parked (trigger unchanged); pytest = ACTIVE. Post-merge chores issued: merge main into feat/mellea, delete stale feat/extraction-mellea + merged feat/extraction.
FULL UNIT-TEST GUIDE delivered 2026-07-20 (house-accurate): repo already has remote_smoke marker + --run-remote-smoke opt-in conftest pattern → Ollama smoke test = @pytest.mark.remote_smoke in tests/smoke/ (NOT generic skipif); coverage auto-runs via addopts (gate disabled per comment); ruff excludes tests/; tests/data/ has federal/state sample docs. Taught: AAA, one-behavior-per-test, independence, parametrize for _locate_span table, fixtures/monkeypatch, -k/-x/--lf workflow, red-first discipline, what-NOT-to-test (prompt strings, LLM intelligence, private churn). Smoke test doubles as delivery vehicle for the owed 3B numbers.
SPLIT EXECUTED (2026-07-20 later): feat/extraction = cleanup done (5 removal commits incl. 6264f33 amended Benchmark.md fix) + main merged in (560ea23; main gained CourtListener client #13) — PR open-status UNKNOWN (gh CLI not installed; if opened, via browser — ASK). User now on feat/mellea @ bca12f4 (pyproject tweak) = correct branch for unit-test work. Restore-directory Q&A concluded: overlay/no-overlay taught, narrow-pathspec habit taught (data/ would resurrect purged PDFs), benchmark/s typo flagged ×2. RESUMED: unit tests per 2026-07-19 lesson (worked example + 8 specs). STILL NEVER DELIVERED: real 3B run numbers (citations/unfound) — extractor has never executed end-to-end; smoke test after unit tests.
PR SPLIT PLAN (2026-07-20): user must PR feat/extraction (~80 commits ahead of main); wants feat/mellea for WIP + cleanup of partial code. Mentor verified: feat/extraction-mellea (all 3 remotes) = STALE ancestor, no unique commits — safe to ignore/delete later; mellea.py NOT imported by extraction/__init__ or tests (grep clean) → git rm safe, pytest to confirm. STRATEGY RULED: snapshot branch + forward-delete + squash-merge (NO history rewrite — 80 interleaved commits, pushed, contains merges; squash makes history noise + test-spin (Q21) + "testig" commit msg die at merge). Steps issued: (0) relocate 2 untracked GT strays, (1) git branch feat/mellea + push personal, (2) git rm WIP files [mellea.py, load_ground_truth.py (line-47 bug), run_benchmarks.py (empty); GT data = user decision], (3) FIX base.py before PR: docuemnt_path typo + contract mismatch w/ EyeciteExtractor is library surface Woody reviews, (4) pytest+ruff+pre-commit all-files, (5) gh pr create --base main --repo gt-csse, squash merge, (6) sync feat/mellea w/ main after. UNIT TESTS resume promised after PR (user's own reminder).
Q43 (2026-07-18) | Strategy-per-class refactor proposed by user (MelleaNaive, MelleaDivideConquer; no mode flags) — ENDORSED w/ teaching: (1) boundary rule args-vs-classes: subclass for behavioral variation (algorithm), ctor arg for data variation (model size) → strategy×model matrix = classes×args, NO MelleaNaive3B class explosion; (2) Template Method: MelleaExtractorBase implements extract_citations pipeline (lines→spans→citations→doc), subclasses implement only abstract _generate_lines(text)->list[str]; keep BaseExtractor thin contract; (3) package layout extraction/mellea/{base,naive,divide_conquer}.py + __init__ re-exports (Q41 import rules apply); (4) name attr encodes strategy+model ("mellea-naive-3b") for cache/bars; (5) SEQUENCING RULE taught: never refactor broken code — yesterday's 5 fixes + running numbers FIRST, then refactor is mechanical w/ regression check (same numbers). Refactor ruled AFTER first green run. | guidance issued
DAY SCOPE 2026-07-17: "just finish Mellea code." Morning review: eager session FINALLY deleted ✓, orchestrator loop appearing ✓, BUT scope drifted to decompose: extract_citations now calls _divide_and_conquer_strategy which returns [] (stub) — extraction structurally empty. SCOPE FORK ruled A: wire naive today (5 fixes), decompose = branch work per user's own TODO. Five fixes issued: (1) call _naive_strategy not divide_and_conquer; (2) _locate_span body + start cursor param (cursor in orchestrator — currently absent); (3) _construct_extractor_citation takes span, kill Span(0,0), loop `continue` on None (current code appends citation even when unfound!); (4) full ExtractedDocument return (still TypeError); (5) classification today = static UNKNOWN, generative _classify_citation parked (per-citation LLM call cost + self-in-signature + Literal[enum] structured-output risk + keyword-arg calling convention). Unused imports L29 (extract_baseline/document_file) = F401 hook block. last_unfound still unsurfaced. 5 compare() tests deferred AGAIN by scope statement (owed).
Q42 CLOSED (2026-07-17): parallel citations — user ran the verification themselves (find_eyecite_bugs.ipynb): eyecite emits ONE FullCaseCitation PER REPORTER REFERENCE (37 F.3d 1225 and 48 F.2d 346 as separate objects sharing plaintiff/defendant). User's multi-object GT convention APPROVED, condition: each object gets its own segment span + matched_text. Decision-record line owed: "GT granularity = one citation object per reporter reference; parallels = N objects, shared parties, distinct spans." MYSTERY SOLVED: source = 'Interstate Commerce Commission v. Brimson, 154 U.S. 447; 155 U.S. 3' in Korematsu — yesterday's misrouted ICC plaintiff edit + wiped region 0d3bfc7e ('155 U.S. 3', empty plaintiff) were the user fixing THIS. Bonus eyecite quirks banked for field-metric later: pin_cite lands in extra ('1227 -28'), reporter 'U. S.' internal space, antecedent_guess garbage. OPEN: what is Plain_Korematsu_v_US.txt (13 hand-typed full cites in ground_truth/ — if intended as GT, Q30 transcription veto applies; if Mellea test fixture, fine but relocate). Sprint status unknown post-deadline — 3B numbers still never delivered; plot.py mtime 14:55 + figures/ exists.
ORGANIZATION NEGOTIATION (2026-07-16 EOD): user rejected mentor's monolithic _lines_to_document, decomposed into _locate_span/_construct_extractor_document/_classify_citation/orchestrator — mentor ACCEPTED their factoring (it's good) and slotted logic into THEIR shape. Key adaptations: _locate_span needs start:int cursor param (per-call independence breaks duplicate "Id." handling — cursor lives in orchestrator); _construct_extractor_document takes span param (kills Span(0,0)); _classify_citation must return UNKNOWN not hardcoded FULL_CASE (honest default; real classification = decompose branch). User set strategy=None (dropped RejectionSampling — accepted for speed, quality note logged). Eager 3B session in __init__ = 3rd non-deletion, bluntly re-ordered. uuid citation_id = good instinct.
RENDERING SUSPICION #4 + FILE DRIFT (2026-07-16 EOD): supplied paste-ready code NOT applied — file instead drifted (4th copy of mapping dict pasted into _construct_extractor_document + Span(0,0) fabricated placeholder; extract_citations returns bare ExtractedDocument() = TypeError kw_only; _naive_strategy still (self) w/ undefined text; eager 3B session still in __init__; new _classify_citation stub hardcodes FULL_CASE). Hypothesis: user never SAW the code message (client swallows long pre-final text). Mitigation: re-sent as FULL-FILE replacement w/ render-confirm marker; explicitly offered "say 'apply it' and I edit the file myself" (contract-change decision handed to user). New stray: Plain_Korematsu_v_US.txt dropped into ground_truth/ (locate note issued, defer). _classify_citation dropped from supplied file (hardcoded lie) — flagged.
SUPPORT DROP (2026-07-16 EOD, deadline): user asked "I just need it" — mentor supplied COMPLETE paste-ready code in chat (no file edits by mentor; contract letter kept). Scope: cleaned __init__, _naive_strategy(text), _lines_to_document full body, extract_citations wiring. DEBRIEF OWED TOMORROW: user must explain span-recovery cursor logic back + write the 5 compare() tests. Progress noted since last review (guard added ✓, text:str restored ✓, cls→self ✓, load_dotenv into property ✓); still present: eager 3B session, _naive_strategy missing text param (both fixed in supplied code). User's "matched_text-only, no spans" idea REJECTED with reason: own __post_init__/Q30 veto + span pairing needs spans; find() cursor gives real spans free. 4th decompose stub appeared (_return_citation) — parked again.
MELLEA REFACTOR REVIEW (2026-07-16 late): CREDIT — circular import fixed correctly (.types direct import), strategy-pattern adopted, base.py → instance methods + ExtractedDocument return. THREE FIRST-RUN BLOCKERS: (1) session property MISSING None-guard → new Ollama session every access; (2) _naive_strategy(self) uses undefined `text` → NameError; (3) __init__ still creates EAGER hardcoded-3B session (L32-34, dead attr `_mellea_session_granite_3b`, nothing references it) → delete. DESIGN RULING ISSUED: ABC contract changed text→document_path — REVERSE IT: runner must read text once and pass same string to all backends (compare()'s text-equality assert depends on it; extractors doing own I/O = divergent-text risk + _load_data returns "" silently). HIERARCHY HALF-CONVERTED as warned: eyecite.py untouched → EyeciteExtractor violates new ABC. SCOPE CREEP flagged: _seperate_by_paragraph + @generative _check_for_citation stubs = decompose strategy under today-deadline; park to feat/mellea-decompose (own TODO says so), ship _naive_strategy. SPELLING: "docuemnt_path" SHIPPED IN ABC PARAM NAME + citions/exactly-how-×2/seperate/bried/structred persist — gate recurrence #7. resolve_citations L70: dropped decorator but first param still named cls.
Q41 (2026-07-16) | CIRCULAR IMPORT found REAL in refactored mellea.py: `from mellea_lrc.extraction import ExtractedDocument` = importing own package __init__ mid-initialization → cycle if __init__ exposes MelleaExtractor. Fix taught: import sibling submodules directly (from .types / mellea_lrc.extraction.types), never own __init__; layering core←extraction is one-directional so core imports are always safe. Adaptor skeleton issued (cursor-based ordered span recovery + full-text fallback + unfound counter, UnknownCitation, backend=MELLEA). Q40 refactor partially applied by user (__init__ w/ model_id, TYPE_CHECKING session import — credit); load_dotenv still at class body. | guidance issued
Q40 (2026-07-16) | MelleaExtractor 3-model design: user proposed one instance var per model; mentor counter: one-model-per-instance (strategy pattern) — __init__(model_id, name), lazy session via property, runner owns the trio in BACKENDS dict. Rationale: per-model connection cost, no 3× method API, add-model = dict entry. RIPPLE flagged: classmethods must become instance methods → BaseExtractor ABC + EyeciteExtractor signatures change too. Pre-checks assigned: (a) do 8B/32B model_id constants exist in mellea.backends.model_ids, (b) ollama list — are models pulled locally (32B on laptop = time risk; adrian-ubuntu remote?). Fix prompt typos while in file (benchmark validity). Q36 folded in: instance .name feeds .cache/<name>/ folder naming. | guidance issued
DEADLINE TRIAGE (2026-07-16): user deferred compare() unit tests to tomorrow — accepted under deadline w/ guard (text-equality assert stays TODAY, it's one line). Support temporarily deepened (worked skeletons, still no full implementations) due to deadline + rendering frustration; revisit level after sprint.
PROGRESS-REPORT framing available: recall-only numbers legitimate w/ caveat "span validation pending; full GT conversion parked."
NOTE: "check GT correctness later" reframed — deserializer built with Q19 assertion + doc hash = mechanical alignment check at every load; human review only for label judgment.
METHODOLOGY FLAG (parked, revisit before reporting numbers): ground truth built by correcting eyecite pre-annotations has systematic blind spot — eyecite's own misses only caught by unaided eye → inflates eyecite recall vs Mellea. Consider: second pass with Mellea pre-annotations merged, or a no-pre-annotation document as control.
NOTE: scripts/label_studio/label_studio.py already encodes LS region format (value.start/end/text) — the export converter is its mirror image; user's "need to look at LS output format" is half-answered by symmetry.
RESUME REVIEW (2026-07-15, session 2): fix-list scoreboard — tuple order FIXED (return now (total,correct), consistent); MOCK label ADDED; membership recall + Q31 normalize NOT applied (plot.py:25-26 still zip + raw ==); spellcheck NOT done → recurrence #6 ("benchamrks" run_benchmarks.py:1, "mateched" plot.py:24, "sytles" plot.py:39 SURVIVED prior flag, "fiile"/"doesn't exists" load_ground_truth.py:42,45, inverted "isn't incorrect" plot.py:39 survived).
Q34 RULED 2026-07-15 (picker): RESURRECT FULL CONVERTER — reverses the 2026-07-14 GT-lite pivot ("too many nuances", report deadline). Rationale for the reversal OWED (one line for decision record: what changed since yesterday?). Prediction-key bug (Q27 trap at load_ground_truth.py:17) still to fix as part of rewrite. citation_groupings reframed: not dead code — it's the grouping seed (LS per-region results share region id); mirror of scripts/label_studio/label_studio.py.
Q29 RULED 2026-07-15 (picker): extend ExtractionBackend with MANUAL="manual". Library change — own commit, separate from converter.
Q35 CLOSED 2026-07-15 | rename committed (9ea302c, clean pure-rename incl. state dirs sweep) — BUT consumer load_ground_truth.py:42 still reads "ground-truth" → file now raises on run. VERIFICATION-GATE RECURRENCE #3: rename committed without running the consumer. Lesson issued: grep old string before declaring rename done.
Q29/enum DONE (cc9c3a0): MANUAL + MELLEA members added, own commit, correct ordering. MELLEA unruled but accepted → spawned Q36.
Q36 (2026-07-15) | 3 Granite sizes (3B/8B/32B) all stamping backend=MELLEA: where in ExtractionMetadata is model size recorded so cached outputs are attributable? User deferred ("user doesn't care about backend") — reasoning CORRECTED: consumer is the benchmark itself (.cache attribution, 3 bars), not the end user. | parked, unparks WITH three-backends idea
HOOK EVIDENCE (2026-07-15): .pre-commit-config.yaml exists (ruff hooks, always_run) but pre-commit never installed — proof: T201 live in repo, both commits passed. Directive: `uv run pre-commit install` (dep at pyproject:67) + add typos/codespell hook. Spelling gate stays manual until then. Ruff + format verified clean repo-wide, so install won't block.
English note issued: enum "members/values" vs "enums" (commit msg cc9c3a0). DoD explained (Definition of Done).
PROGRESS (2026-07-15 late morning): path fix VERIFIED (mentor ran: exit 0 — but citations=[] so run proves only path+JSON parse); T201 print removed + backend=MANUAL applied unprompted (credit); Q22 commit accepted (a74eba3, pushed) — CLOSED; svg deleted + figures/ mkdir'd BUT half-executed: plot.py:65 still saves bare-relative "recall_1.svg" (needs working_dir/"figures"/... path) and .gitignore lacks figures/ entry. "fiile"/"doesn't exists" still in load_ground_truth.py:41,44 — 3rd flag, delegated to typos hook. Process note logged: "Done, take a look" ≠ gate; user runs + pastes.
PROCESS INCIDENT #2 (2026-07-16): user committed converter (950a6f7) OVER standing "do not commit" blocker — line 47 prediction key, Q19 assert, iterdir filter all still missing (only ruff cleanup done; hooks enforced that). Then reported "LS content is wrong, LS broken, need alternative, don't insist" — mentor reframed w/ evidence: the "wrong content" = the 42-citation prediction copy (23 rejected FPs present, 10 human adds absent); real labels unread under ["result"]. Falsifiable prediction issued: fix line 47 → 29 citations, wrongness mostly evaporates. Q25 ghost invoked (LS broke unexplained before; cause never captured). If content errors survive the 29: export JSON directly editable (relabel/delete span-safe; Q30 veto was transcription only).
Q38 (2026-07-16) | SPAN-PAIRING DESIGN — user proposed sort-by-span + span comparison; mentor CREDITED & legitimized Q31 reopening with the missing argument: duplicate short citations ("Id.") make text membership meaningless. Framework taught: (1) pairing = span OVERLAP not exact equality, sorted sweep; (2) detection = binary on overlap (partial-plaintiff is a TRUE positive for detection); (3) field quality = parked metric #2 — NO fractional recall. Two answers owed by user: overlap rule (any vs threshold), and Q39. | open
Q39 (2026-07-16) | Does MelleaExtractor emit real char spans, or text-only (spans would need recovery by search)? If no spans, span pairing biased against Mellea — needs fallback rule. User to inspect and report. | open
BENCHMARK PLAN issued (2026-07-16, user requested planning help; "use what we have, plots even if data wrong (for now)"): Q39 ANSWERED by mentor reading mellea.py — extract_citations returns response.split('\n') = raw strings, NO spans/kinds/ExtractedDocument (eyecite path is complete). Plan: (C-first) compare(ground,predicted)->RecallResult dataclass {total_gt, matched, missed list, spurious}, text-equality assert FIRST LINE, sorted sweep + 1:1 greedy any-overlap pairing as named fn, 5 unit tests FIRST (pytest must-have idea partially unparked: zip-trap, dup-Id, partial overlap, spurious, empty); (A) run_benchmarks runner (doc,backend)->.cache/<backend>/<dochash>.json, reuse generate_test_cases plumbing (Q6/Q28 partially ripe — 3rd consumer of save/load/mapping = move to library), skip-if-cached + --force, folder naming mellea-3b/8b/32b (Q36 adjacent); (B) Mellea adaptor: span recovery via ordered text search + Q31 normalize as key, unfindable lines = logged spurious, all UnknownCitation (typed recall impossible for Mellea → detection recall is metric #1), FIX FIRST: class-level session at mellea.py:21 (import-time Ollama connect; lazy + parametrize model_id — three-backends design note now RIPE). Order: compare+tests → eyecite runner → plot wired (MOCK mandatory; PREDICTION on record: eyecite vs 42-copy pseudo-GT ≈100% by construction) → Mellea adaptor → GT reconciliation parallel.
SPELLING IN LLM PROMPT found: mellea.py L45-49 "citions", "exactly how exactly how", docstring "bried" — errors inside instruct() can degrade extraction quality = mechanical argument for typos hook (still absent from yaml). Chat: "comparission" #2, "safe"→save. Line 47 = 7th demand (framed: same effort, meaningful signal for step-3 pipeline test).
LS INCIDENT ESCALATION (2026-07-16 afternoon): save at 14:04 WIPED annotation 12 from 29→13 regions (timeline: 07-14 02:40 saved 29; 13:58 PATCH 200 still ~29; 14:04 wipe). Mentor's "crash is cosmetic" verdict CORRECTED: first save in a session works, subsequent saves from crashed mobx state serialize partial tree = DESTRUCTIVE. Misrouting confirmed via picker: user typed "Interstate Commerce Commission" into a DIFFERENT region; it landed on Chastleton. Committed 29-region export = intact backup (versioned-GT decision paid off). PICKER RULINGS: user deleted SOME regions on purpose; recovery route = OFFLINE JSON EDITS (LS = viewer only until Woody upgrades past 1.23.0; bug note owed).
FORENSICS (span overlap): ALL 16 removed regions STANDALONE — zero duplicates; incl. 3 distinct Hirabayashi full-cites, 7 supras, 3 short-cites. Only 'months' SupraCitation = clearly correct delete (mislabel). MENTION-LEVEL vs CASE-LEVEL GT taught: Short/Supra/Id kinds exist because every mention counts; "already labeled above" deletions must be reversed. OWED: user's per-region keep/delete list w/ rationale.
RECONCILIATION PROCEDURE issued: base=committed 29-file → justified deletes → field edits in JSON (ICC to correct region, L. Assn. fix) → single canonical file in ground_truth/ → commit → line 47 fix → converter+Q19 validates → count. Cleanup: archive project-8.json, move t.py out of ground_truth/.
LS SAGA CLOSED (2026-07-16): PATCH /api/annotations/12 → 200 CONFIRMED (user captured request detail). Verdict: saves ALWAYS worked; LS 1.23.0 frontend mobx crash masks success. Q25 retroactively ANSWERED (never broken server-side). Workflow: edit→Update→hard-refresh. Woody note owed: LS 1.23.0 dead-node crash, UI-only, upgrade check. Investigation arc banked as lesson: belief→console→network→request detail (each step = measurement replacing belief).
GT CONTENT ERRORS spotted in PATCH body: (1) Chastleton region plaintiff="Home Bldg. & L. Assn." — likely user's test edit (confirm & revert) else real error; (2) Blaisdell region plaintiff="L. Assn." — genuine eyecite truncation in GT, fixable now. CORRECTION SWEEP path: fix in LS → re-export → replace committed LB_Korematsu_v_US.json → recommit; hash-cache auto-invalidates (design earning its keep).
Cookie redaction reminder #2 (new sessionid pasted again).
LINE 47 = 6th demand — sole remaining gate; expected next message: count=29.
NETWORK EVIDENCE (2026-07-16, 2nd capture): LS SAGA NEAR-CLOSED — annotation endpoint 12?taskID=21&project=8 → 200 (×2, method col missing; user to confirm PATCH via request detail), drafts POST → 201 (typed content safe in draft regardless), NO 4xx anywhere → schema-mismatch DEAD as edit-blocker explanation. Conclusion: server accepts saves; mobx frontend crash (LS 1.23.0, version from sentry baggage header) makes saves LOOK failed. Workflow if persistence check passes: edit→Update→hard-refresh. Owed: persistence check report (3rd ask), method confirm. Security habit taught: user pasted live sessionid+csrftoken into chat — redact cookies in shared captures; suggest re-login. Lesson banked: console=client witness, network=server witness. Line 47 = 5th demand, framed as last gate to first honest recall number.
CONSOLE EVIDENCE (2026-07-16, user pasted): NO 4xx visible — mentor's 400-naming-parenthetical prediction NOT confirmed (scored against mentor honestly). Sentry ERR_BLOCKED_BY_CLIENT = ad-blocker noise. REAL SIGNAL: mobx-state-tree dead-node crash in POST-SAVE cleanup (updateAnnotation → loadTask/setLSFTask replaces tree → dropDraft/sendUserGenerate on dead '/annotationStore/annotations/0') = LS FRONTEND BUG, version-specific. KEY HYPOTHESIS: edits may have been SAVING server-side all along, UI crash masks success. Tests issued: (1) Network tab PATCH status (console≠network lesson taught), (2) persistence check: hard-refresh reopen / re-export grep. Workaround if confirmed: edit→Update→hard-refresh; or LS upgrade. Line 47 = 4th demand.
Q23 REVISITED w/ EVIDENCE (2026-07-16): user asked why LS won't let them edit citation fields (pasted upload_schema.py + config XML). Mentor verified: _field_results (label_studio.py:59-71) emits ALL asdict fields incl None; export PREDICTION section contains parenthetical×42 + publisher×11 (NOT in config); RESULT section clean (only config names) AND saved successfully historically (29 labels, 17 plaintiffs exist). So mismatch PROVEN but not yet proven to be today's edit blocker. Decisive test (re-issued, owed since 07-13): devtools Network tab on field edit + Update — 400 body names offending from_name; user to also identify symptom class (a) panel absent (b) read-only (c) update fails (d) update succeeds/export unchanged. Fix if confirmed: add parenthetical+publisher TextAreas to XML + re-run upload_schema (non-destructive); cleanup: filter None in _field_results.
NEW GAPS from export probe: (1) config lacks parenthetical → GT structurally blind to parentheticals → metric-#2/methodology-flag entry; decide w/ Woody. (2) notes in config but NOT a dataclass field → first filled Notes = TypeError at mapping[kind](**params) — converter fragility now has concrete trigger. (3) RESULT=29 labels confirmed intact & rich — line 47 fix (Q37) STILL owed, 3rd demand.
EVIDENCE STANDOFF (2026-07-16): user maintains "LS content wrong, I see the errs in raw JSON" — export contains BOTH result (human) and prediction.result (eyecite) copies; demanded ONE example entry + its key path (falsifiable either way). Line 47 fix + 29-count paste STILL owed (2nd demand).
English: "comparission"→comparison (gate warning), "errs"→errors, embedded-question word order ("how we know", not "how do we know").
PREDICATE RE-LITIGATION FLAGGED (2026-07-16): user proposed exact match — Q31 already ruled ws-normalize 2026-07-14; reopening requires stated reason. Field-by-field reframed as SECOND METRIC (parsing accuracy over matched pairs), not recall predicate — folding fields into recall corrupts the number. Parked.
CONVERTER REWRITE delivered 2026-07-15 evening — BLOCKED at review. Q37: line 47 STILL the Q27 prediction key; cache output = 42 citations (eyecite copy) stamped backend=MANUAL = false provenance. User's own print showed 42 vs known 29 target, unnoticed (lesson: read output against expectation). Must-fix list issued: (1) ["result"] + rerun expect 29, (2) Q19 assertion MISSING from DoD — add per-span assert, text extraction must move above loop, (3) next(iterdir()) at L116 can grab .cache dir — worked by fs luck, filter *.json, (4) ruff 3 errors will block commit (hooks NOW INSTALLED, verified): T201 L85 ("Thelength" spelling), ERA001 L52+L57 — L52 landmine taught: uncommenting it exhausts citations_by_type → empty main loop; comprehension directive renewed. DEBT logged: dead if L75-76, duplicate mapping (import from generate_test_cases), textarea from_name→ctor TypeError fragility, SourceFormat.TEXT→PDF flip unconfirmed, cache location extends layout decision (append to record), spellcheck hook STILL not in yaml ("doesn't exists" L112 survives). CREDIT: module-level I/O fixed, grouping sound, hash-cache good instinct. Iterator/filter exchange: user diagnosed laziness+exhaustion+mutation correctly; double-filter fix works but fragile; advice/advise english note issued. Cache-key insufficiency noted (hashes input only, not code).
CONVERTER DoD issued 2026-07-15 (see chat): correct key → group by region id → kind mapping to CanonicalCitation subclasses → Q19 per-span assertion + doc hash → backend=MANUAL → no module-level I/O → run it (verification gate).
Housekeeping open: Q25 (LS fix one-liner), Q22 rename uncommitted, recall_1.svg untracked (regenerable — decision record says don't commit; gitignore candidate), ruff T201 load_ground_truth.py:22, spellcheck tooling (recurrence #6 — mechanical hook demanded).
IDEA WATCH: "Three Mellea backends (Granite 3B/8B/32B)" MUST-HAVE approaches ripeness — GT file committed (5af98c0); ripens fully when converter produces a valid ExtractedDocument. Remember lazy-vs-eager session design note.

## Decisions
<!-- date | decision | pointer to user's written rationale -->
2026-07-09 | Serialize CanonicalCitation discriminated union by keeping `kind` a ClassVar (preserves type invariant — can't mutate FullCase→ShortCase) and injecting `kind` into the dict only at the serialization boundary; load uses a kind→class `mapping` to rebuild. Rejected: making `kind` an instance var (would let callers change a citation's type). | rationale given in chat 2026-07-09, reviewed = sound
2026-07-09 | Benchmark data layout: manual ground-truth citations in benchmarks/data/ground_truth/ (version-controlled, scripts never write there); model outputs (mellea, eyecite) in .cache/{mellea,eyecite}, regenerated whenever a model changes, gitignored. Rejected (in chat, to be appended to record): 3 sibling folders under data/ all in git; committing regenerable model outputs. | user's record delivered w/ reread confirmation; "rejected" sentence owed

## English log
<!-- rule | user's example | recurrence count -->
spelling — spell-check shipping text before it lands | "indentifying"/"inesrt"/"coresponding" (decision record); "findinds" (chat); "rand" (chat); "run_bechmarks.py" (SHIPPED FILENAME, survived reread+commit); "genereate" (chat) | 4 — GATE ACTIVE; reread demonstrably insufficient, mechanical spellchecker recommended twice
precedent vs precedence — "takes lower precedence" (priority), "precedent" = prior case; high-stakes in legal domain | "it takes less presedent" | 1 — CORRECTED: used "takes lower precedence" correctly next message; rule landed

## Ideas
<!-- idea | must-have / nice-to-have (user assigns) | status: parked / ripe (mentor flags when timing is right) / done -->
- Corpus manifest instead of vendored PDFs | MUST-HAVE (user assigned 2026-07-20) | parked — replace committed legal PDFs with corpus_manifest.json (CourtListener opinion IDs + SHA-256 of fetched docs) + fetch_corpus.py; fallback govinfo.gov w/ source URL in manifest. Rationale (external advice user received): opinion text = public domain (Georgia v. Public.Resource.Org 2020) BUT publisher PDFs/West headnotes copyrighted + Google Scholar ToS forbids bulk retrieval; manifest also gives frozen corpus (input-drift vs extraction-regression distinction) + slims git history. NOTE: main's new CourtListener client (#13) is the enabling infra; tests/data/ PDFs went INTO main via the merged PR = existing target for this cleanup. Unpark trigger: corpus expands beyond Korematsu OR before any public/broad release. Escalation path if IBM-adjacent: Paul/Nathan for real legal review.
- Two-machine environment-setup sync | MUST-HAVE | UNPARKED 2026-07-20/21 (machine-switch trigger fired). Inventory findings: .claude/ GITIGNORED → mentor-state.md does NOT travel via repo (chezmoi add it — under $HOME so legal); 3 branches have NO upstream (feat/mellea, feat/mellea-tests, feat/legal-documents) + feat/extraction ahead 8 → `git push personal --all` required; uncommitted pyproject/uv.lock + untracked tests/data/ + 2 GT strays to settle first; gitignored valuables needing explicit decisions: .env (encrypt), plotting/ (hero chart code, ignored BY CHOICE → won't travel), notebooks/, ai-hallucination-dataset-layer3/; chezmoi INSTALLED, manages ~/.claude/skills/mentor-mode (SKILL.md travels ✓) + .zshrc, source repo clean — verify it has a pushed remote; ~/.claude project memory is machine-local → chezmoi-add memory dir if same project path on machine B; age key = bootstrap secret, manual secure transfer only. Full checklist issued in chat; user to write the setup README in chezmoi WHILE executing (that was the original idea). — mentor recommendation: NOT the extra-file-in-mirror plan (accidental-push risk, cites test-spin 64e9dc3 incident); instead chezmoi (user already uses it, 94b4124) for dotfiles + age-encrypted .env + the setup README itself; VS Code built-in Settings Sync for editor settings; secrets never in plain git even private.
- Hero-style benchmark bar chart (lives at benchmarks/Extraction/plotting/, gitignored by choice) | nice-to-have | parked — ripens when benchmark numbers exist. NOTE: .gitignore change adding plotting/ still uncommitted as of 2026-07-13.
- Three Mellea session backends to test IBM Granite 4.1 3B / 8B / 32B | MUST-HAVE (user assigned 2026-07-13) | parked — ripens once ground_truth/ is populated. Design note for implementation time: MelleaExtractor builds its session at CLASS level (runs at import); three class-level sessions = 3 model connections on import — decide lazy vs eager then.
- Pytest suite for the whole extraction code | must-have (user: "we need", 2026-07-13) | UNPARKED 2026-07-19 (user asked "should I unit test the Mellea function? teach me") — lesson delivered: testability seam (LLM = boundary, test below via monkeypatched _naive_strategy; lazy session = instantiation w/o Ollama is the payoff), house style = test_extraction.py (flat functions, SAMPLE constants), worked example + 8-spec list issued, smoke test → tests/smoke/ w/ skipif. Mellea commit 3fef58e landed; file evolved again (_assemble_canonical_citation/_assemble_extractor_citation **kwargs). 5 compare() tests = sibling debt, next.

## Next actions
<!-- concrete next step so a fresh session resumes cold -->
- DoD MET: ExtractedDocument round-trips save→load to an equal object (verified: full main() run printed nothing).
- Adversarial pass DONE. 5 findings; user ruled: #1 accept (added ReferenceCitation+UnknownCitation to mapping), #2 accept (enum keys + CitationKind(kind) lookup), #3 accept (redesigned to citation_id-keyed kind_mappings — order-independent), #4 accept (deepcopy, renamed citation_copy), #5 REJECT (raw KeyError acceptable in small codebase; documented via comment L111). Rulings sound.
- MILESTONE CLOSED 2026-07-09: DoD met — full ExtractedDocument round-trips save→load to equal object; post-fix run verified clean. Adversarial pass complete, all findings ruled.
- Lesson PROMOTED to skill 2026-07-09: "never validate through the transformation under test" added to Verification gate in SKILL.md; user committed to chezmoi (94b4124). SESSION CLOSED.
- Next session candidates: (a) move save_citations/load_citations out of benchmark script into the library (design question: where does serialization belong?); (b) rejected finding #5 stands as accepted debt.
- Recall-question pool: asdict-equality blindness; dataclass __eq__ class check; str-Enum == str; .value vs .name; why kind stayed a ClassVar; GIT (added 2026-07-20 after 3/4 quiz): `git restore <f>` vs `git restore --staged <f>` — user confused unstage with discard; retest in a few days with the edit→add→edit-more scenario (where bare restore DESTROYS unstaged work).
- GIT THREE-TREES lesson delivered 2026-07-20 (user demanded "force me to learn"): model + table + 4-question picker quiz. Results: Q1 ✗ (chose `restore file.py` for unstaging — the dangerous confusion), Q2 ✓ reset --soft, Q3 ✓ revert for pushed, Q4 ✓ reset --hard danger. Tie-in taught: yesterday's MM mellea.py — bare restore would have wiped the unstaged ruff fixes.
- Bug diagnosed (mentor confirmed via read): load L93 passes `source_metadata` (a SourceMetadata dataclass from DocumentBase) straight through `**data` as a raw dict → object != but asdict ==. User to discover via Q3/Q4 and fix by reconstructing SourceMetadata (note nested SourceFormat str-Enum).
- Teach/bank: never validate a round-trip with asdict equality — it flattens nested dataclasses and hides dict-vs-dataclass mismatches. Compare live objects.
- Offered readings: python dataclasses docs (asdict recursion, __eq__ same-class); Real Python "Data Classes in Python 3.7+".